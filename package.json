{
  "name": "campaigner_app",
  "version": "1.0.0",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "build": "gleam build --target javascript && node scripts/build.js",
    "start": "npm run build && electron .",
    "electron": "electron .",
    "test": "gleam test",
    "lint": "gleam format --check src test",
    "format": "gleam format src test"
  },
  "devDependencies": {
    "c8": "^11.0.0",
    "electron": "^35.0.0"
  }
}
