#!/bin/bash
set -e

echo "Running pre-commit checks..."

PROJECT_ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$PROJECT_ROOT/campaigner_app"
echo "Running verification pipeline (lint -> test -> coverage)..."
make check-coverage

echo "All checks passed!"